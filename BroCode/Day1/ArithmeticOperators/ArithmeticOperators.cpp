#include <iostream>

int main()
{

    // int students = 20;

    // students = students + 1;
    // students += 1;
    // students++;

    // students = students - 1;
    // students -= 1;
    // students--;

    // students = students * 2;
    // students *= 2;

    // students = students / 3;
    // students /= 3;

    // int remainder = students % 2;

    // parenthesis ()
    // multiplication & division * /
    // addition & subtraction + -

    int students = 6 - (5 + 4) * 3 / 2;

    std::cout << students << '\n';

    return 0;
}